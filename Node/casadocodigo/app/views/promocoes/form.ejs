<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Formulario Promocoes</title>
</head>
<body>
    <div class="erros">
        <%if(errosValidacao){%>
            <ul>
                <%for(i=0;i<errosValidacao.length;i++){%>
                <li><%=errosValidacao[i].param%> - <%=errosValidacao[i].msg%></li>
                <%}%>
            </ul>
        <%}%>
    </div>
    <form action="/promocoes" method="post">
        <div>
            <input type="text" name="mensagem"/>
        </div>

        <div>
            <select name="livro[id]">
                <%for(i=0; i<lista.length;i++){%>
                <option value="<%=lista[i].id%>"><%=lista[i].titulo%></option>
                <%}%>
            </select>
        </div>


        <input type="submit" value="Cadastrar Promoção Relampago">
    </form>
</body>
</html>